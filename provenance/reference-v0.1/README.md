# Historical reference inventories

These unchanged inventories describe the Python reference implementation at
commit `5dce6e72e93a165913f38713626b8c3e5c8a8f1b`. They are retained for lineage
and comparison, not used as the native implementation's capability or schema
authority. Current contracts are under `spec/` and current execution evidence
is recorded in `artifacts/native-verification.json`.
