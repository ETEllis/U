# U implementation authority

Edward authorized the complete build on 2026-09-14. The preserved GPT-6 Pro dossier and execution directive define the end-state architecture; provenance/review/RECONCILIATION.md qualifies earlier reviews. Explicit current user instructions govern scope. Preserve supplied files byte-for-byte and record new implementation decisions separately.

Implement coherent executable paths and test actual mechanisms. Never call syntax coverage execution, tests proofs, simulators devices, or a partial release the complete language. Keep all original release gates visible. CDC predates U; preserve its pinned native semantics and the independent BiDi oracle. Edward's subsequent instruction explicitly authorizes a coordinated public-facing README, landing-page and paper refinement in the sibling BiDi checkout; keep its executable semantics intact unless required and tested separately.

Use subagents for independent bounded work. Workers are not alone; edit only owned modules, coordinate interfaces, and do not revert other work. The root owns integration, releases and commits. The current instruction requires migrating the substantive implementation to U with reproducible self-compilation and removing Python from the normal build/runtime path. Preserve the Python reference under an explicit historical/bootstrap boundary for independent differential tests. Native support primitives must expose their implementation and limits. Host eval/exec of U or source-language payloads is forbidden.

Language implementation source lives in compiler/, stdlib/, cdc/ and tools/ as U programs; bin/etellis-u is the public launcher. tests/native_programs/ executes U fixtures, with independent unittest and Node harnesses for adversarial comparison. Document exact supported fragments, backend contracts and bootstrap trust. Package and profile versions are separate; execution side effects require capabilities. Paper theorem scope must match proved statements; empirical numbers come only from fresh receipts.
