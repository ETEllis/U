# U implementation authority

Edward authorized the complete build on 2026-09-14. The preserved GPT-6 Pro dossier and execution directive define the end-state architecture; provenance/review/RECONCILIATION.md qualifies earlier reviews. Explicit current user instructions govern scope. Preserve supplied files byte-for-byte and record new implementation decisions separately.

Implement coherent executable paths and test actual mechanisms. Never call syntax coverage execution, tests proofs, simulators devices, or a partial release the complete language. Keep all original release gates visible. CDC predates U; preserve its pinned native semantics and the independent BiDi oracle. Do not modify the sibling BiDi checkout.

Use subagents for independent bounded work. Workers are not alone; edit only owned modules, coordinate interfaces, and do not revert other work. The root owns integration, releases and commits. Python 3.13+ is the explicit stage-0 reference bootstrap in this environment; no Rust toolchain is installed. The small total checker, graph semantics, domain realizations, effects, evidence and CLI stay independently inspectable. Host eval/exec of U or source-language payloads is forbidden.

Tests use the standard unittest runner. Source modules live in u/, executable in bin/etellis-u. Document exact supported fragments, backend contracts and bootstrap trust. Package and profile versions are separate; execution side effects require capabilities. Paper theorem scope must match proved statements; empirical numbers come only from fresh receipts.
